PORT=4000

# Optional: instead of config/serviceAccountKey.json, paste the entire
# service account JSON (as a single line) into this env var for cloud deployments.
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account", ...}
